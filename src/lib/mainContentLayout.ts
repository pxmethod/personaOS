/** Shared horizontal bounds for primary views (directory, overlap, compare). */
export const MAIN_CONTENT_OUTER = 'mx-auto w-full max-w-6xl px-4 py-8 sm:px-8'

/** Persona detail body padding inside the bottom sheet (applied on the scroll layer, not the frosted backdrop). */
export const MAIN_CONTENT_DETAIL_GUTTERS = 'px-4 py-8 sm:px-10 lg:px-14'
